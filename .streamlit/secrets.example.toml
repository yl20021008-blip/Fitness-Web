# 不要把真实 secrets.toml 上传 GitHub
# 在 Streamlit Cloud 的 App settings -> Secrets 里填写同样内容

[supabase]
url = "https://你的项目id.supabase.co"
anon_key = "你的 anon public key"
service_role_key = "你的 service_role key，只给 admin_dashboard.py 使用"

[admin]
password = "你自己设置一个后台密码"
